/**
 * @param {number[]} num
 * @param {number} k
 * @return {number[]}
 */
var addToArrayForm = function(num, k) {
return String(BigInt(String(num).replaceAll(",",""))+BigInt(k)).split('').map(Number)
};
